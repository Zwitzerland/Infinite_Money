# Context Chats

Place exported chat transcripts (markdown, text, or JSON) in this directory. The ingestion script parses turns and records SHA256 checksums. Avoid storing secrets or personally identifiable information.
