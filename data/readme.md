# Data

Local datasets live here and are intentionally ignored by Git.

Place sample or generated inputs under `data/` and keep secrets or licensed
data out of the repository. Use `docs/LOCAL_SETUP.md` for dataset export
instructions.
